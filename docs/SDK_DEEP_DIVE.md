# Historical Note

This file previously documented reverse-engineering work against the Claude Agent SDK.

That runtime no longer exists in this branch. The Codex-native port uses the Codex CLI runner in `container/agent-runner/src/index.ts` instead.
